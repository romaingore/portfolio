<section class="bg-gray-50 py-12 dark:bg-gray-900">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <p
            class="text-center text-base font-semibold text-gray-600 dark:text-gray-400"
        >
            Trusted by over 1,000+ startups worldwide
        </p>
        <div class="mt-6 grid grid-cols-2 gap-8 md:grid-cols-4 lg:grid-cols-5">
            <!-- Placeholder Logos using simple squares with text for now -->
            {
                ["Acme", "Tuple", "Mirage", "StaticKit", "Workcation"].map(
                    (company) => (
                        <div class="col-span-1 flex justify-center md:col-span-1">
                            <span class="flex items-center justify-center text-xl font-bold text-gray-400 dark:text-gray-500">
                                {company}
                            </span>
                        </div>
                    ),
                )
            }
        </div>
    </div>
</section>
